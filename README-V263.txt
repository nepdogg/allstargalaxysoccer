ALLSTAR GALAXY V263 — UNIVERSAL PHONE/PC FRAMEWORK REPAIR

This update is based on the latest complete repository supplied by the user.

FIXES
- Repairs pages that stop scrolling before the footer on iPhone Safari.
- Clears stale body/html/site-shell scroll locks only when no modal is open.
- Makes all seven mobile navigation buttons equal, stable, and fully clickable.
- Prevents decorative glow, ticker, title, and hero layers from intercepting taps/clicks.
- Reduces Search clipping and keeps it completely inside the viewport.
- Restores page-matched glow and staggered pulse on the header logos and active button.
- Removes the orange Shuffle hover leak from non-Shuffle pages.
- Brightens the silver/platinum status rails while retaining opposite directions.
- Keeps hero dots clickable and above visual layers.
- Applies the same header dimensions and hit areas to every public page.

UPLOAD
Upload the contents of the V263 update package into the repository root, preserving css and js folders. Replace matching HTML files.
After GitHub Pages deploys, close and reopen the iPhone Safari tab or clear the site cache so the new v263 CSS/JS loads.
