ALLSTAR GALAXY V261 — UNIVERSAL TOP FRAMEWORK

This release locks one shared responsive system from the top of every public page through the hero image.

Included:
- One desktop and phone header layout across Home, Team, Updates, Media, Shuffle, Live, Follow, About, Game Awards, Season Archive, and 404.
- Page-color hover states for every navigation button. Shuffle is orange only on the Shuffle page.
- Restored staggered pulse: left logo, active page button, right logo/Search.
- Identical ticker dimensions and alignment.
- Universal hero aspect ratios and matching carousel indicator controls on every rotating hero.
- Mobile height/overflow resets so pages remain scrollable.

Upload all files from the update package to the repository root and preserve the css folder.
