ALLSTAR GALAXY V240 — FINAL PHONE HOMEPAGE LOCK

Upload index.html and the css folder to the ROOT of the GitHub repository.
Preserve the css folder and replace matching files.

Changes:
- Locks the Homepage Shuffle navigation button to gold.
- Locks the Homepage Galaxy Shuffle card, title, icon, live badge, border,
  background and hover glow to gold.
- Keeps the standalone Galaxy Shuffle page orange.
- Makes the phone navigation title visibly taller.
- Leaves navigation sizing, Search, ticker, hero, cards and desktop layout unchanged.

After GitHub Pages deploys, close and reopen the browser tab or perform a hard refresh.
