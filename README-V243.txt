ALLSTAR GALAXY V243 — REPAIRED PHONE HOMEPAGE

Upload index.html and the css folder to the root of the GitHub repository.
Replace matching files and preserve the folder structure.

V243 repairs the malformed stylesheet tag introduced in V242, removes the
visible stylesheet text, replaces the V240/V241/V242 override chain with one
clean stylesheet, keeps Home as the only active navigation button, forces the
homepage Shuffle button/card to gold, restores a balanced phone title height,
and removes accidental top spacing.
