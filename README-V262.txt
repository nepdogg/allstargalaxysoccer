ALLSTAR GALAXY V262 — UNIVERSAL FRAME STABILIZATION

This update locks the shared public-page framework from the top of the page through the hero on phone and PC.

Fixes:
- page-colored logo glow and staggered logo/active-button/Search pulse
- full navigation hitboxes remain clickable immediately
- decorative glow layers cannot intercept pointer events
- Shuffle hover inherits the current page color outside the Shuffle page
- brighter silver/white ticker energy pulses travel in opposite directions
- one compact mobile header and one desktop header across public pages
- galaxy background restored around every site shell, including Shuffle
- hero frame/dots normalized; Updates and Shuffle now receive hero controls
- fixed-height and overflow scroll locks reset so pages can reach the footer

Upload all included files to the repository root and preserve folders.
