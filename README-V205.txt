ALLSTAR GALAXY V205 — STICKY NAVIGATION + MOBILE HEADER FIT

Changes:
- Keeps Home and Media section navigation visible while scrolling on phones.
- Measures the sticky site header automatically and positions section navigation directly below it.
- Keeps desktop section navigation fixed at the side.
- Preserves full navigation words instead of ambiguous icon-only or single-letter labels.
- Uses seven equal-width main-navigation buttons on phones.
- Gives SCHEDULE enough room with refined responsive typography and spacing.
- Makes the navigation-title graphic taller and vertically centered between both logos.
- Updates CSS and section-navigation browser cache references to V205.
- Code files only. No PNG/JPG graphics and no master-content.json.

Upload the contents of this ZIP to the repository root and replace matching files while preserving folders.
